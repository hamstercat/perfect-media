<UserControl x:Class="PerfectMedia.UI.TvShows.Shows.TvShowFanart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:img="clr-namespace:PerfectMedia.UI.Images"
             xmlns:shows="clr-namespace:PerfectMedia.UI.TvShows.Shows"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=shows:TvShowImagesViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid MinWidth="650">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <img:SelectableImage Grid.Column="0" DataContext="{Binding FanartUrl}" WidthRatio="1920" HeightRatio="1080" />
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <img:SelectableImage Grid.Row="0" DataContext="{Binding PosterUrl}" MaxWidth="300" WidthRatio="680" HeightRatio="1000" />
                    <img:SelectableImage Grid.Row="1" DataContext="{Binding BannerUrl}" MaxWidth="300" WidthRatio="758" HeightRatio="140" />
                </Grid>
            </Grid>
            <Grid Grid.Row="1">
                <ItemsControl ItemsSource="{Binding SeasonImages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Width="250">
                                <TextBlock Text="{Binding SeasonNumber, Converter={StaticResource SeasonNumberToNameConverter}}" />
                                <img:SelectableImage DataContext="{Binding PosterUrl}" MaxWidth="250" WidthRatio="680" HeightRatio="1000" />
                                <img:SelectableImage DataContext="{Binding BannerUrl}" MaxWidth="250" WidthRatio="758" HeightRatio="140" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
